services:
  - type: web
    name: rust-backend
    env: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/rust_backend
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: RUST_LOG
        value: info
